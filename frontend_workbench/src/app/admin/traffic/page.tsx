import LiveTrafficFeed from "./LiveTrafficFeed";

export default function TrafficPage() {
  return (
    <div className="animate-in fade-in zoom-in-95 duration-500 font-sans">
      <LiveTrafficFeed />
    </div>
  );
}
